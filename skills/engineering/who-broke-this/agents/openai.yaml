interface:
  display_name: "Who Broke This"
  short_description: "Review staged changes and verify fixes with Codex"
  default_prompt: "Use $who-broke-this to review the staged changes and verify the fixes."

policy:
  allow_implicit_invocation: false
